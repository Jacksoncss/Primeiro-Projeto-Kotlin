<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_unipe"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_unipe"
        android:theme="@style/Theme.MeuCurso">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SobreCursoActivity"
            android:exported="false"
            android:label="@string/titulo_sobre" />

        <activity
            android:name=".DisciplinasActivity"
            android:exported="false"
            android:label="@string/titulo_disciplinas" />

    </application>

</manifest>
